/**
 * Benny's Ballista — input, meters, menus, minimap.
 *
 * STEP 2 SLICE: empty. Space/Return input, the ammo scan list, the two
 * meters and the minimap all arrive with the shot pipeline (work-order steps
 * 4-5). This stub exists now purely so js/main.js's bootstrap loop — copied
 * from Race Tracks, which always drives RT.ui — has something to call.
 */
RT.ui = (function () {
  'use strict';

  function init() {}
  function tick(dt) {}

  return { init, tick };
})();
